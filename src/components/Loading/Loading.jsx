import React from 'react'
import styles from './Loading.module.scss'

function Loading(props) {
  return <div className={styles.container}>Chargement en cours...</div>
}

export default Loading
